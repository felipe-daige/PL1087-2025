{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "engines": {
        "node": "22.x"
    },
    "scripts": {
        "build": "vite build && npm run postbuild",
        "postbuild": "cp public/build/.vite/manifest.json public/build/manifest.json 2>/dev/null || true",
        "dev": "vite",
        "vercel-build": "npm install && npm run build"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    },
    "dependencies": {
        "chart.js": "^4.5.1"
    }
}
